<html>
    <head>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-eOJMYsd53ii+scO/bJGFsiCZc+5NDVN2yr8+0RDqr0Ql0h+rP48ckxlpbzKgwra6" crossorigin="anonymous">
    </head>

    <body>
        <div class="container">
            <p>Dear <%= @user.name%></p>
            
            <% if @dec == "accept"%>
                 <p>Your request to rent a space at <%= @request.host.address%> was <span class="text-success">accepted</span></p>
                 <p>Please contact your <%= link_to "host",@request.host%> to coordinate</p>

            <%else%>
                <p>Your request to rent a space at <%= @request.host.address%> was <span class="text-danger">denied</span></p>
                <p>We encourage you to keep looking for storage spaces <%= link_to "here",'/listings'%></p>
            <%end%>

             <div class="card w-50">
            <div class="card-body">
                <h5 class="card-title">Request Details</h5>
                <table class="table">
                    <thead>
                        <th scope="col">Address</th>
                        <th scope="col">Host</th>
                        <th scope="col">Duration</th>
                        <th scope="col"> # Boxes</th>
                        <th scope="col">Price</th>
                    </thead>
                    <tbody>
                        <tr>
                            <td><%= @request.host.address%></td>
                            <td><%= link_to @request.host.name,@request.host%></td>
                            <td><%= @request.duration%></td>
                            <td><%= @request.boxes%></td>
                            <td><%= @price%></td>
                        </tr>
                    </tbody>
                </table>
            </div>
        </div>
        <p><strong>Thank you for using AirStorage!</strong></p>
        </div>
        
    </body>
</html>